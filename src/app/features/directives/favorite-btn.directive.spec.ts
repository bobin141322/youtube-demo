import { FavoriteBtnDirective } from './favorite-btn.directive';

describe('FavoriteBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoriteBtnDirective();
    expect(directive).toBeTruthy();
  });
});
